{
  "name": "defiscore-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for DeFi credit scoring with ZK proofs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:localhost": "hardhat run deploy.js --network localhost",
    "deploy:mumbai": "hardhat run deploy.js --network mumbai",
    "deploy:polygon": "hardhat run deploy.js --network polygon",
    "deploy:lending": "hardhat run scripts/deploy-lending-escrow.js --network localhost",
    "deploy:lending:mumbai": "hardhat run scripts/deploy-lending-escrow.js --network mumbai",
    "verify:mumbai": "hardhat verify --network mumbai",
    "verify:polygon": "hardhat verify --network polygon",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  }
}
